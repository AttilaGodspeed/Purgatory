using UnityEngine;

// This class is just used to hold a set of references to objects
// so EventManager can quickly look them up.
public class objectReferences : MonoBehaviour {
    public GameObject[] references;
}
